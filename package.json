{
  "name": "monorepo",
  "workspaces": [
    "api",
    "client"
  ],
  "scripts": {
    "start": "concurrently --kill-others --names \"ESLint,StyleLint,JsonServerAPI,Vite\" --prefix \"[{name}]\" --prefix-colors \"bgRed.bold,bgGrey.bold,bgBlue.bold,bgYellow.bold\" --max-processes=\"75%\" \"npm run eslint\" \"npm run stylelint\" \"npm run start:api\" \"npm run start:client",
    "start:client": "npm run --workspace client dev",
    "start:api": "npm run --workspace api start",
    "eslint": "eslint ./client/src/**/*.{ts,tsx,json,cjs,mjs,js}",
    "stylelint": "stylelint -config ./.stylelintrc.js --config-basedir ./client ./client/src/**/*.scss"
  },
  "devDependencies": {
    "vite-plugin-checker": "^0.6.2"
  }
}

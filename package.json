{
  "scripts": {
    "start": "live-server --port=8080 --host=127.0.0.1 --watch=index.html,./js/,./css/ --entry-file=index.html --no-css-inject --wait=500"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
